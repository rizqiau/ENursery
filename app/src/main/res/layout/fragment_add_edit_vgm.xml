<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:visibility="gone" />

        <!-- TextView untuk hasil scan barcode (idBibit) -->
        <TextView
            android:id="@+id/tvIdBibit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="A001"
            android:padding="8dp"
            android:textColor="@android:color/black"
            android:textStyle="bold" />

        <!-- Spinner untuk pilih Plot -->
        <Spinner
            android:id="@+id/spinnerPlot"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Spinner untuk pilih Batch -->
        <Spinner
            android:id="@+id/spinnerBatch"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Input Tinggi Tanaman -->
        <EditText
            android:id="@+id/etTinggi"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Tinggi Tanaman (cm)"
            android:inputType="numberDecimal" />

        <!-- Input Diameter Batang -->
        <EditText
            android:id="@+id/etDiameter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Diameter Batang (cm)"
            android:inputType="numberDecimal" />

        <!-- Input Jumlah Daun -->
        <EditText
            android:id="@+id/etJumlahDaun"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Jumlah Daun"
            android:inputType="number" />

        <!-- Upload Foto -->
        <ImageView
            android:id="@+id/imgBibit"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_marginTop="12dp"
            android:background="@color/black"
            android:scaleType="centerCrop" />

        <Button
            android:id="@+id/btnUploadFoto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Upload Foto" />

        <!-- Tombol Submit -->
        <Button
            android:id="@+id/btnSubmit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Simpan Data VGM"
            android:layout_marginTop="16dp" />

    </LinearLayout>

</ScrollView>
