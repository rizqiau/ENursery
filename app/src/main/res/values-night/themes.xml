<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ENursery" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Warna utama aplikasi -->
        <item name="colorPrimary">@color/main_dark</item>
        <item name="colorPrimaryVariant">@color/main_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/main_dark</item>

        <!-- Warna background utama -->
        <item name="android:colorBackground">@color/bg_dark</item>

        <!-- Warna yang digunakan di BottomNavigationView -->
        <item name="colorSurface">@color/my_bottom_nav_bg_dark</item>
        <item name="colorOnSurface">@color/my_bottom_nav_icon_dark</item>
    </style>

    <style name="Theme.ENursery" parent="Base.Theme.ENursery" />
</resources>
